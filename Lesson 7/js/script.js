"use strict"; //запись для использования строгого стиля кода

something = 10;

console.log(something);

let number = 5;
/**
 * let - изменяемая переменная
 */

const leftBorberWidth = 1;
/**
 * const - постоянная (неизменяемая переменная)
 */

/**
 * snake_case - один из вариантов названия файлов и папок
 * UPPER_SNAKE_CASE - обозначают переменные которые хотят видеть в виде констант
 * Kebab-case
 * PascalCase - используется для названия классов в JS
 */

const COLOR_RED = "#f00";